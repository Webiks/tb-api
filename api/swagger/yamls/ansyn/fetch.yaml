x-swagger-router-controller: ansyn

post:
  tags:
  - Ansyn
  description: Fetch layers for ansyn.
  operationId: fetchLayers
  parameters:
  - name: payload
    description: The name of the world
    in: body
    required: true
    schema:
      $ref: "./definition.yaml#/definitions/FetchLayers"
  responses:
    "200":
      description: Fetch Success
      schema:
        $ref: "./definition.yaml#/definitions/FetchSuccess"
    default:
      description: Error
      schema:
        $ref: "./definition.yaml#/definitions/ErrorResponse"

